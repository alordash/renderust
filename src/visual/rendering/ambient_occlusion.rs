use crate::visual::drawing_buffer::DrawingBuffer;

pub fn ambient_occlusion(canvas: &mut DrawingBuffer) {
    for x in 0..canvas.get_width() {
        for y in 0..canvas.get_height() {
            
        }
    }
}